services:
  web:
    build:
      context: .
    container_name: idea-workbench
    restart: unless-stopped
    environment:
      PORT: "8080"
      IDEA_DESK_REQUIRE_AUTH: "${IDEA_DESK_REQUIRE_AUTH:-false}"
      IDEA_DESK_USERNAME: "${IDEA_DESK_USERNAME:-}"
      IDEA_DESK_PASSWORD: "${IDEA_DESK_PASSWORD:-}"
    ports:
      - "127.0.0.1:8184:8080"
    volumes:
      - ./data:/app/data
